email_regex: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i

model:
  user:
    email_length_max: 255
    name_length_max: 50
    password_length_min: 6
help:
  user:
    avatar_size: 80
paginate:
  manage: 6
